import React from "react";
import { render, screen } from "@testing-library/react";

import Organograma from ".";

describe("Organograma Component", () => {
  it("renders without crashing", () => {
    // render(<Organograma />);
  });
});
